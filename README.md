# Blog API

A RESTful CRUD API for managing blog posts, built with Laravel 12.

## Tech Stack

- Laravel 12
- SQLite
- Pest (Testing)

## Setup
```bash
composer install
cp .env.example .env
php artisan key:generate
php artisan migrate
php artisan serve
```
